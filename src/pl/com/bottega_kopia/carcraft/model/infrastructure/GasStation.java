package pl.com.bottega_kopia.carcraft.model.infrastructure;

/**
 * Created by macie on 13.11.2016.
 */
public class GasStation {
/*
    public void fullService(Car car) {
        double amount = car.getFuelcapacity() - car.getFuelLevel();

        car.fill(amount);

        if (car instanceof HybridCar){
            HybridCar hybrid = (HybridCar) car; //Jastem pewien, że HybridCar jest instancją Car więc rzutujemy
            double kwh = hybrid.getEnergyCapacity() - hybrid.getEnergyLewel();
            hybrid.charge(kwh);
        }

    }
*/
}
